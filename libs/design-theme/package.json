{
  "name": "@th-m/design-theme",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./theme.css": "./src/theme.css"
  },
  "scripts": {
    "generate-theme": "bun run scripts/generate-theme.ts",
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "vitest": "latest"
  },
  "nx": {
    "name": "design-theme",
    "sourceRoot": "libs/design-theme/src",
    "projectType": "library",
    "tags": ["kind:lib", "runtime:universal", "domain:design"],
    "targets": {
      "generate-theme": { "outputs": ["{projectRoot}/src/theme.css"] },
      "typecheck": { "outputs": [] },
      "test": { "outputs": [] }
    }
  }
}

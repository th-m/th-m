# Building an LLM — Working Outline

## Purpose

Explain the input → training → model and embeddings → inference pipeline as one
continuous next-token prediction system.

## Flow

1. Text becomes tokens.
2. Training error changes weights.
3. Embeddings and transformer layers carry learned patterns.
4. Inference predicts and appends one token at a time.
5. Generated output still requires interpretation and evaluation.
